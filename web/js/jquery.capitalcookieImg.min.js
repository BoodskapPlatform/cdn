! function(o) {
    var e = {};
    o.capitalimgcookie = function(n) {
        "true" != function(o) {
            var e, n, t, a = document.cookie.split(";");
            for (e = 0; e < a.length; e++)
                if (n = a[e].substr(0, a[e].indexOf("=")), t = a[e].substr(a[e].indexOf("=") + 1), (n = n.replace(/^\s+|\s+$/g, "")) == o) return unescape(t)
        }((e = o.extend({}, {
            mainMessage: "",
            acceptButton: "Aceptar",
            expirationDays: 7,
            backgroundColor: "#666",
            fontSize: "14px",
            fontColor: "white",
            btnBackgroundColor: "#f2a920",
            btnFontSize: "11px",
            btnFontColor: "white",
            linkFontColor: "#ffff00",
            cookieName: "capitalimgcookie"
        }, n)).cookieName) && o(document).ready(function() {
            var n;
            n = '<div id="capital-img" class="raisingpopup"><span class="msg">' + e.mainMessage + '<a href="" class="btn-aceptar button-close">' + e.acceptButton + "</a></span></div>", o("body").append(n),
             o("#capital-img a.btn-aceptar").on("click", function() {
                return function(o, e, n) {
                    var t = new Date;
                    t.setDate(t.getDate() + n);
                    var a = escape(e) + (null == n ? "" : "; expires=" + t.toUTCString());
                    document.cookie = o + "=" + a
                }(e.cookieName, !0, e.expirationDays), o("#capital-img").remove(), !1
            })
        })
    }
}(jQuery);